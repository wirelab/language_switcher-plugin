<div class="{{ div_class }}">
  <button class="{{ button_class }}" type="button" data-toggle="dropdown">
  	{{ button_title|raw }}
  </button>
  <ul class="{{ ul_class }}">
  	{% for locale in locales %}
  		<li><a href="{{ locale.url }}">{{ locale.name }}</a></li>
  	{% endfor %}
  </ul>
</div>