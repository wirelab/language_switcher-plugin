<li class="{{ div_class }}">
	<a href="#" class="{{ button_class }}" data-toggle="dropdown">{{ button_title|raw }}</a>
	<ul class="{{ ul_class }}">
	  	{% for locale in locales %}
	  		<li><a href="{{ locale.url }}">{{ locale.name }}</a></li>
	  	{% endfor %}
	</ul>
</li>